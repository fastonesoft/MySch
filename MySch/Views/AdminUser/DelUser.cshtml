@model MySch.Bll.BllAcc

@{
    Layout = null;
    //AdminUser/DelUser
}

<script type="text/javascript">
    $(function () {
        $('#dialog-form').dialog({
            title: '用户删除',
            width: 410,
            height: 270,
            closable: false,
            closed: false,
            cache: false,
            modal: true,
            buttons: [{
                text: '删除',
                iconCls: 'icon-ok',
                handler: function () {
                    //直接进行删除操作
                    $.post('/AdminUser/DelTokey', $('form').serialize(), function (d) {
                        if (d.error) {
                            $.messager.alert('错误提示', d.message, 'error');
                        } else {
                            //关窗口
                            $('#dialog-form').dialogClose();
                            //更新网格
                            var row = $('#users-grid').datagrid('getSelected');
                            var index = $('#users-grid').datagrid('getRowIndex', row);
                            $('#users-grid').datagrid('deleteRow', index);
                        }
                    });
                }
            }, {
                text: '取消',
                iconCls: 'icon-no',
                handler: function () {
                    $('#dialog-form').dialogClose();
                }
            }],
            onClose: function () {
                //启用按钮
                $('.easyui-linkbutton').linkbutton('enable');
                //清除提示
                $('div.tooltip').remove();
                $('div.combo-p').remove();
            },
        });
    })

</script>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    <div style="padding:20px 20px 0px 20px;">
        <div class="form-item-row">
            <div>@Html.LabelFor(a => a.IDS)：</div>
            <span class="textbox">
                @Html.TextBoxFor(a => a.IDS, new { @class = "textbox-text", @readonly = "readonly" })
            </span>
            @Html.ValidationMessageFor(a => a.IDS)
        </div>
        <div class="form-item-row">
            <div>@Html.LabelFor(a => a.Name)：</div>
            <span class="textbox">
                @Html.TextBoxFor(a => a.Name, new { @class = "textbox-text", @readonly = "readonly" })
            </span>
            @Html.ValidationMessageFor(a => a.Name)
        </div>
        <div class="form-item-row">
            <div>@Html.LabelFor(a => a.Pwd)：</div>
            <span class="textbox">
                @Html.TextBoxFor(a => a.Pwd, new { @class = "textbox-text", @readonly = "readonly" })
            </span>
            @Html.ValidationMessageFor(a => a.Pwd)
        </div>
        <div class="form-item-row">
            <div>@Html.LabelFor(a => a.Fixed)：@Html.CheckBoxFor(a => a.Fixed, new { @disabled = "disabled" })</div>
        </div>
    </div>
    @Html.HiddenFor(a => a.ID)
}
